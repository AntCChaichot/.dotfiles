On Debian:
- Change /etc/apt/sources.list

(Add contrib and non-free repos)
do in softwares & updates
sudo apt update
sudo apt -y install apt-file
sudo apt-file update

(Nvidia Card)
sudo apt install -y nvidia-detect
sudo nvidia-detect
sudo apt install -y nvidia-driver (according to the recommendation)

(Install Thai Fonts)
sudo apt -y install fonts-tlwg-loma

(Install Vim)
sudo apt -y install vim

(Install wget to pull from repo)
sudo apt -y install wget

- Install Zoom Meetings

(Install Build-essentials for some programs)
sudo apt -y install build-essential dkms linux-headers-$(uname-r)

(Install extra fonts)
sudo apt -y install ttf-mscorefonts-installer rar unrar libavcodec-extra gstreamer1.0-libav gstreamer1.0-plugins-ugly gstreamer1.0-vaapi

(Install Vlc)
sudo apt -y install vlc

(Install firewall and enable)
sudo apt -y install ufw
sudo ufw enable
(Check Status)
sudo ufw status verbose
sudo ufw status numbered
(Allowing and remove permission from firewall)
sudo ufw allow ssh
sudo ufw delete 1 --> check from numbered

(Install ssh)
sudo apt -y install ssh

(Install Power saving)
sudo apt -y install tlp

(Install Google Chrome)
wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
sudo apt -y install ./google-chrome-stable_current_amd64.deb

(Install KeePassXC)
sudo apt-get -y install keepassxc

visit https://extensions.gnome.org/
- App Icons Taskbar
- Blur My shell
- Dash to Dock (disable show overview on startup)
- panel corners
- NoAnnoyance v2
- gesture improvements (and https://github.com/harshadgavali/gnome-x11-gesture-daemon)
- Forge
- Hanabi
- User themes: 
    visit https://www.gnome-look.org/browse/
	--> in ~/.themes
    Tokyo Night borderless
	Sweet Dark
	Ant-Dracula
	--> in ~/.icons
	Candy-icons
    Tela-circle
    capitaine-cursors
- GRUB boot
    tela grub theme
    sleek orange

(Set shortcuts)
ctrl+alt+esc for xkill (unbind other esc first to set)
ctrl+alt+t for terminal (command = gnome-terminal)

(If all done = clean up)
sudo apt update && sudo apt upgrade
sudo reboot
